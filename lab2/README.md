# Module 2 Lab — Python HTTP Client

This lab uses Python and the `requests` library to send an HTTPS GET request and display the HTTP status code, response headers, and a content preview.

Run the script with:

```bash
python httpCycle.py