# Module 4 Lab — Flask Application

This lab creates and runs a simple Flask web application in Codio.

## Run

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py

Then open the Codio preview on port 5000.
